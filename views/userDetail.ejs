<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %> 
<body>
    <%- include('partials/header') %>
    <h1><%= title %> </h1>
    <div class="card text-center" >
      <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs">
          <li class="nav-item">
            <a class="nav-link " href="/userList">Usuario</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/userDetail/<%= user.id %>">Detalle</a>
          </li>
        </ul>
      </div class="card-body">
        <div class="card-body">
          <h5 style="text-align:left">Número usuario: <%= user.id %></h5>
          <h4 class="card-title"><%= user.last_name %> <%= user.name %></h4>
          <p class="card-text"><%= user.email %></p>
          <div class="btn-group" role="group">
            <a href="/userEdit/<%= user.id %>">
              <button type="button" class="btn btn-success btn btn-default" style="margin: 10px">Editar</button>
            </a>
            <form action="/userDelete/<%= user.id %>?_method=DELETE" method="POST">
              <button type="submit" class="btn btn-danger btn btn-default" style="margin: 10px">Eliminar</button>
            </form>
          </div>
        </div>
      </div>
    </div>
</body>
</html>